const utils = {
  getParameter(){
    return JSON.parse(
      decodeURIComponent(atob(location.hash.slice(1)))
    );
  }
};
document.addEventListener('DOMContentLoaded', () => {
  document.getElementsByTagName('footer')[0].innerHTML += `Generated by Shiny Central @ ${new Date().toISOString()} / © 2019 Shiny Project`;
});

